fastapi==0.115.5
uvicorn==0.32.0
sqlalchemy==2.0.36
pydantic==2.9.2
python-dotenv==1.2.1
cryptography==46.0.4
# Bundled so the image can talk to Postgres without a rebuild: SQLite is
# fine for a single box but does not survive platforms that move containers.
psycopg[binary]==3.2.12
